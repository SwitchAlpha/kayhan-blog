## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem it solves. If the reason is not obvious from the diff, it belongs here. -->

## Checks

- [ ] `pnpm test` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` succeeds
- [ ] Schema change? A migration is included and `pnpm db:migrate` applies cleanly
